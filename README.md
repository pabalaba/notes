# Note cli app 

Implementare un app per scrivere note usando come database un json.

### Per installare il progetto:
```javascript
npm install
```

### Per usare l'app:
```javascript
node app.mjs --help
```